# Host: Frank (Linux)
# Apply with: home-manager switch --flake .#Frank
{ ... }: {
  imports = [
    ../common
  ];

  # Host-specific overrides go here.
  # e.g. extra packages, different settings, etc.
}
